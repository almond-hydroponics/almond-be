setup:
  addons:
    - plan: mongolab
      as: MONGOLAB_ONYX
    - plan: heroku-redis
      as: REDIS
build:
  docker:
    web: Dockerfile
#run:
#  web:  npm config set unsafe-perm true && yarn start
